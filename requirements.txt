PyOpenGL==3.1.7
# Optional: a C extension that speeds PyOpenGL up. Pinned ahead of PyOpenGL
# itself on purpose -- 3.1.7 was compiled against the numpy 1.x C ABI and dies
# with "numpy.dtype size changed" on numpy 2. If it fails to build on your
# system, just drop this line; nothing here depends on it.
PyOpenGL-accelerate==3.1.10
